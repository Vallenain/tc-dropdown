import { DropdownHostDirective } from './dropdown-host.directive';

describe('DropdownHostDirective', () => {
  it('should create an instance', () => {
    const directive = new DropdownHostDirective();
    expect(directive).toBeTruthy();
  });
});
